import { Button } from "@nextui-org/react";

export default function ShareIDGenerateButton() {
  const onGenerateClick = () => { };

  return (
    <Button onClick={onGenerateClick} color="primary">
      シェアIDを発行する
    </Button>
  );
}
