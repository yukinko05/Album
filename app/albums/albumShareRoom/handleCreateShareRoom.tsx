import { Button } from "@nextui-org/react";

export default function CreateShareRoomPage() {

	const handleCreateShareRoom = () => {

	};

	return (
		<Button onClick={handleCreateShareRoom} color="primary">
			シェアルームを作成する
		</Button>
	);
}
